# 滤波器设计

## 一些例题

>>>例题 1
The first order low pass filter has the transfer function

$$$
H(s) = \frac{1}{1+\tau s}, \text{where $\tau$=1 is the time constant.}
$$$

### Find the impulse invariant digital filter having a sampling interval of 0.05s.

Putting τ=1, analog frequency response function:

$$$
H(\omega) = \frac{1}{1+j\omega}
$$$

### Specify the difference equation if the sampling interval is changed to 0.5s.

>>>
